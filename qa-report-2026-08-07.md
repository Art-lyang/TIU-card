# TIU_CARD Daily QA Report — 2026-08-07

> 자동 스케줄 실행. 5개 서브에이전트(integrity-verifier / i18n-text-auditor / asset-reference-checker / card-balance-checker / narrative-consistency-checker) 병렬 진단 결과 통합.

## 한눈에 보기

| 구분 | 카운트 |
|-----|-------|
| ✅ 잘된 것 | 35+ 항목 (전 영역 안정) |
| ⚠️ 체크할 것 | 10 항목 |
| 🔧 개선할 것 | 10 항목 (P0 1 / P1 3 / P2 4 / P3 3) |
| 🆕 새 이슈 | P0 1건(오늘 발견), P1 3건(재확인) |

**P0 (즉시 조치)**
- **[i18n] `LOG-RELIEF-SUPPLY` / `LOG-RELIEF-INSPECT` / `LOG-RELIEF-MORALE` — EN 오버레이 완전 누락.** 최근 밸런스 패치(`3ac4b54`/`e3ac5b0`/`e38340f`)에서 신규 12개 카드가 이 3개 LOG를 생산하는데 `oracleLogs` EN 오버레이가 짝지어지지 않음. EN 모드에서 로그 뷰어·지휘관 일지·증거 조합 화면이 한국어 원문 노출 → CLAUDE.md "Mission i18n Rule" 위반.

**전체 통합 상태**
- `node tools/validator.js` 자동 12항목 **전량 PASS**, 이슈 0건.
- 카드 592장·LOG 420(생산)/285(정의)/159(소비)·미션 19·엔딩 16·아카이브 116 참조 무결성 100%.
- 에셋: IMG/BGM/SFX/CCTV 스팅 영상 9/9 + navcam 10/10 매칭, 고아 후보 6건(캐릭터 감정 v1) 존재.
- i18n: 카드 640/640, 뉴스 57/57, 이브닝 316/316, 아카이브 113/113, 미션 dossier 19/19 완전 커버 — 예외는 위 P0 3건.
- 밸런스: GI 폭주 0건, 지배 전략 사실상 없음, priority 라벨-실체 이원화 이슈 확인.

## 에이전트별 핵심 발견

### 1. integrity-verifier
- validator 자동 12+항목 전량 PASS(이슈 0건), fx 스키마 1216건 100% 정합, req/cond try-catch 전 구간 방어.
- 크리티컬 발견 없음. 문서-코드 드리프트(LOG ID 네임스페이스 확장)만 정보성 지적.

### 2. i18n-text-auditor
- **[P0] LOG-RELIEF-* 3건 EN 오버레이 누락**(신규 relief 카드군). 로그 뷰어에서 EN 모드 사용자에게 KO 노출.
- **[P1] validator DATA_FILES 목록에 `kd-arc/ys-arc/ij-arc/evening-*` 등 8개 파일 미포함** → 신규 캐릭터 아크 카드 정적검증 사각지대(감사 자체는 640/640 OK 확인).
- **[P3] FP-FE-* 카드 EN 이중 소스**(`lang-content-en-all.js` vs `data-facility.js` FACILITY_I18N_EN) — 드리프트 잠재.

### 3. asset-reference-checker
- IMG/BGM/SFX/CCTV/navcam 완전 매칭, 누락 0건. root↔demo 미러 바이트 단위 일치.
- **[P2] 고아 후보 6건**(캐릭터 감정 `_v1.webp`, doyun/haeun × 3 표정) + demo 미러까지 12개 파일.
- **[P2] `ASSET-IMAGE-REGEN-TODO.md` 문서-코드 드리프트** — CCTV 영상 9/9 완료됐는데 문서는 "brainseeker 1개뿐"으로 남아 재작업 위험.

### 4. card-balance-checker
- **[P1] `C-YS-EQUIP`(data-cards-ys-arc.js:14)** priority "중"인데 `r:-8` 단일축 이상치(전체 풀 최댓값). critical-band 완화가 r>25 구간에 안 걸림.
- **[P2] Act3/4 중간 저항(GI -5~-34) 안전망 공백** — early-resistance-floor는 Act≤2 한정, RH-SAFE-01은 g≤-35 한정.
- **[P2] `FP-*` 자동생성 카드(data-cards-facility-propose.js) 밸런스 미검증** — 감사 공백.
- **[P3] `C-091` 약지배**(좌가 c/r/o/g 전 축 우월), ORACLE 순응 트랙 편도 비대칭.

### 5. narrative-consistency-checker
- **[P1] 세션 팩 오분류로 베버/포스터 코어 이브닝 챗 침묵 위험** — `data-session-decks.js:248-267` `getEveningSessionDeckPack()`이 캐릭터 이름 매칭(`indexOf('베버')`, `indexOf('포스터')`)만으로 두 인물의 모든 이브닝 챗을 `PROMETHEUS_TENSION` 세션 팩에 귀속시킴. LOG-080/081은 코어 확정 트랙(CH-005 / LJC-PROM 무관)인데, 팩 미선택 회차(약 1/3)에서 이브닝 대화가 통째로 소실됨. "선택이 흔적을 남긴다"는 서사 계약 훼손.
- **[P2] 박소영 이브닝 라인(data-core.js:250-255) day 범위 중복**(day 34) — 우선순위 로직 확인 필요.
- 캐릭터 사망/이탈(하은/도윤/세진) 게이팅 정상, trust 4단계 톤 정상, Act 시점 경계 GDD v13과 일치.

## 통합 우선순위

### P0 (즉시)
1. **[i18n] LOG-RELIEF-SUPPLY / LOG-RELIEF-INSPECT / LOG-RELIEF-MORALE EN 오버레이 3건 추가** — `lang-content-en-all.js` 또는 `lang-cards-flow-en.js`의 `oracleLogs` 블록.

### P1 (이번 주)
2. **[narrative] `data-session-decks.js:260-266` 베버/포스터 이름 매칭 폴백 제거 또는 `LOG-080`/`LOG-081` 존재 시 무조건 통과 예외 추가.**
3. **[balance] `C-YS-EQUIP`(data-cards-ys-arc.js:14) 단일축 r:-8 재검토** — priority 상향 또는 값 축소.
4. **[integrity/i18n] `tools/validator.js` DATA_FILES 목록 8개 파일 보강** — kd-arc/ys-arc/ij-arc/evening-ys-arc/evening-ij-arc/evening-emotions/evening-extra-3/research.

### P2 (백로그)
5. **[asset] 캐릭터 감정 `_v1.webp` 고아 6건(root+demo 12파일) 삭제 여부 확인.**
6. **[balance] `FP-*` 자동생성 카드(`FACILITY_EXPANSIONS`) 밸런스 별도 감사.**
7. **[balance] Act3/4 중간 저항 성향(GI -5~-34) 안전망 사각지대 재확인 — 의도적이면 문서화.**
8. **[narrative] 박소영 이브닝 라인 day 34 중복 확인.**
9. **[docs] `ASSET-IMAGE-REGEN-TODO.md` CCTV 영상 완료 상태 반영.**

### P3
10. **[balance] `C-091` 약지배 소폭 보정, ORACLE 순응 트랙 편도 비대칭 설계 문서화.**
11. **[i18n] `FP-FE-*` EN 이중 소스 단일화(`lang-content-en-all.js` 우선 사용 중).**
12. **[docs] priority "상"이 파워 등급이 아니라 서사 비중을 겸하는 관행 문서화 — 미션 게이트 카드는 별도 태그 검토.**

## 🎮 게임성 종합 평가

카드 592장·LOG 420·미션 19·엔딩 16 규모에서 참조 무결성 100%·에셋 결손 0건·i18n 커버율 실질 99.5%는 유사 장르(Cultist Simulator, Papers Please, Reigns) 대비 상위권 관리 수준이다. `req`/`cond` 시스템 레벨 try-catch 방어와 validator 커밋 게이트 이중화가 콘텐츠 확장 속도를 뒷받침한다.

하지만 오늘 발견은 콘텐츠 규모 증가에 따라 **"연결 조직"에서 균열이 시작되고 있음**을 시사한다:
- 신규 relief 카드군은 카드/msg만 EN 있고 그 카드가 만드는 LOG는 EN 없음 → 카드→로그 2단계 참조 누락.
- 세션 팩이 이름 문자열 휴리스틱으로 이브닝 챗을 게이팅 → 코어 확정 이벤트가 무작위 팩 추첨에 삼켜짐.
- 캐릭터 아크 파일(kd/ys/ij-arc)이 validator 사각지대에서 조용히 성장 중.

플레이어 체감으로는 "EN 모드 상시 KO 노출"과 "확정한 선택의 이브닝 대화가 없음"이 가장 아프다 — 둘 다 "이 게임이 내 진행을 제대로 추적하고 있다"는 신뢰를 흔든다. GI 폭주가 없고 지배 전략도 사실상 부재하다는 강점이 이런 신뢰 결손으로 상쇄되지 않도록 P0/P1 우선 처리 권장.

## 📊 타게임 비교 종합

- **Reigns**: ±1 극단적 미니멀리즘으로 밸런스 안전 강제. TIU는 축당 최대 ±8까지 허용하는 대신 `balance-tuning.js` 댐퍼로 저위험 구간에서만 그리드 스냅 — 방향은 합리적이나 안전망 적용 범위(Act, GI 구간)에 사각지대(위 P2).
- **Slay the Spire**: 카드 등급이 곧 파워 레벨과 직결되어 예측 가능. TIU의 `priority`는 파워 라벨과 서사 라벨을 겸하려다 `C-091~095`처럼 "상인데 약함" 현상 발생 — 태그 분리 권장.
- **Cultist Simulator**: 문자열 ID 그래프 구조가 유사하나 모드 커뮤니티에서 참조 무결성 이슈 다발. TIU는 validator를 커밋 게이트로 강제해 구조적으로 더 엄격 — 다만 캐릭터 아크 파일이 목록에서 빠져 있는 게 이번 리포트에서 처음 드러난 균열.
- **Citizen Sleeper**: 확정된 서사 노드를 클럭 네임스페이스로 무작위 콘텐츠와 절대 겹치지 않게 분리. TIU도 `LOG-080`/`LOG-081`처럼 "코어 확정"인 사실은 `coreConfirmed:true` 같은 별도 네임스페이스로 세션 팩 게이트와 원천 격리하는 것이 이번 P1 재발 방지 관점에서 참고할 만하다.
- **Papers, Please**: 서류 항목 1:1 다국어 매핑으로 카드→로그 2단계 참조 누락이 구조적으로 불가. TIU의 현 폴백(EN 없으면 KO 노출)은 QA 검출은 쉬우나 상용 노출 위험 — validator 룰에 "카드가 생산하는 LOG는 EN 오버레이도 존재해야 함" 자동 검증 추가 권장.
- **Disco Elysium**: 텍스트 결손 시 스킬 자체가 등장 안 하는 방식. TIU는 반대(폴백 KO 노출)로 오늘 P0 사례가 정확히 그 함정.

## 다음 액션 제안

1. **P0 즉시**: `lang-cards-flow-en.js` 또는 `lang-content-en-all.js`의 `oracleLogs`에 LOG-RELIEF-{SUPPLY,INSPECT,MORALE} EN 3건 추가 → `node tools/i18n-smoke.js` 재실행.
2. **P1 이번 주**:
   - `data-session-decks.js:260-266`에서 베버/포스터 이름 매칭 로직 리팩터. LOG-080/081 존재 시 세션 팩 게이트 무시 예외 추가.
   - `C-YS-EQUIP` fx 재조정 또는 priority 상향.
   - `tools/validator.js` DATA_FILES 목록 8개 파일 보강.
3. **P2 백로그화**:
   - 캐릭터 감정 v1 웹프 고아 처리(사용자 승인 후 삭제).
   - FP-* 밸런스 감사.
   - Act3/4 중간 저항 안전망 정책 결정 및 문서화.
   - 박소영 이브닝 day 34 중복 확인.
4. **validator 강화 로드맵**:
   - "카드가 log:를 생산하면 그 LOG id의 oracleLogs EN 오버레이 존재 여부"를 validator 룰로 추가 → 오늘 P0 유형 CI 자동 차단.
5. **문서 갱신**: `ASSET-IMAGE-REGEN-TODO.md` CCTV 완료 상태 반영, priority 라벨 의미 이원화 명시.

---
_자동 스케줄로 5개 서브에이전트 병렬 진단 후 생성됨. 코드 수정 없음._
