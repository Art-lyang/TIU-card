# TIU-CARD 출시 후 업데이트 로드맵

> 출시(유료) 전에는 신규 시스템을 넣지 않는다. 아래는 **출시 후** 확장용으로 보류한 항목.
> 출처: GPT 세계관 전달 강화안(2026-07-22) 검토 결과 + 세션 논의.
> 원칙: 제안서 철학("설명 말고 사건으로, 선택이 흔적을 남기고, 모순된 기록을 대조")은 옳고,
>       이 게임은 이미 상당 부분 구현함. 아래는 **진짜 새것**과 **큰 전략 결정**만 추림.

## 이미 구현된 것 (재구축 금지 — 참고용)

- 선택 흔적: 카드 → LOG(`ORACLE_LOGS`) → 아카이브(`data-archive.js`) → 뉴스(`genNewsHeadlines`) → 루트(`transRoute`) → 이브닝/일지 반영
- 선택 기반 아카이브 해금: `data-archive.js` 전부 `unlock: function(logs){...}` (로그=선택 결과)
- 플레이 성향별 카드 풀: 세션덱(`pick_session_deck`)·ACTIVE_SPECS·`transReq` 루트게이트·순응덱(`data-cards-act23-pressure.js`)
- 후속 카드 체인: `chainQueue`(즉시)·`req` day 게이트(day≥X)·미션·변이체 관찰→결정 체인
- 다층 기록 텍스처: 뉴스=공식 / 카드 msg=현장 / LOG(hidden·접근제한)=제한
- 실패→세계관: 엔딩·상태 기반 LOG
- 지휘관 일지 무드: 루트(`JOURNAL_MOODS_ROUTE`)·아크이벤트·이브닝/대화 유대(`JOURNAL_MOODS_BOND`)

## P1 — 출시 후 우선 (게임이 팔리기 시작하면)

### R1. 사건ID 기반 LOG "조사 보드" ★제안서 최고 아이디어
- 지금 LOG 뷰어(주제 필터·일지)를 **사건 단위 조사 보드**로 확장.
- 기능: 진행률(예 3/6), 확보 기록 출처 태그, 모순 문장 강조, 빈 슬롯(미확보), 선택별 영구 미확보 가능.
- 규모: 새 UI 시스템. 큰 작업.
- **검증 먼저**: "회색 신고 버튼 사건" 1개만 프로토타입 → 재미 붙는지 확인 후 확장(제안서 §16).

### R2. 사건 단위 다층 기록 정형화
- 같은 사건을 공식/현장/제한 3줄로 명시 대조(예: 공식=피해 없음 / 증언=3가구 이주 / 내부평가=안정성 +3.2%).
- 지금은 텍스처만 있음 → R1 조사 보드와 함께 사건 단위로 묶기.

### R3. "정확히 N일 뒤" 지연 카드 스케줄러
- 현재 chainQueue(즉시)·req(day≥X)로 근사. 전용 예약 큐는 없음.
- 선택 → `{card_id, delay_days}` 예약 → 세이브 유지 → 중복 실행 방지.
- R1/R2를 자연스럽게 만들려면 필요. 중간 규모.

## P2 — 확장 (선택)

- R4. 카드 이미지 출처 라벨화(SOURCE: CCTV/제보/회수본…). 전 카드 마이그레이션이라 큼.
- R5. 세력별 동일 사건 해석(DG/Meridian/정부/현장) 정형화.
- R6. 실패 사례 기록 해금 확대.
- R7. ARC 종료 지휘 결과서 / 공유용 엔딩 이미지 / 숨겨진 코드.

## 큰 전략 결정 (패치 아님 — 새 캠페인/모드)

### D1. 생활안전센터 / 민간 ARC 방향
- 제안서의 생활안전센터·회색 버튼·제12생활구·ARC 01~04(물류시장/검문허브/관찰병동)는
  **지금 게임(지휘관·봉쇄·이변체·간부진·ORACLE)과 결이 다른 민간 행정 호러**.
- TIU 마스터 볼트 캐논엔 있으나 카드게임엔 없는 레이어 → 넣으면 사실상 새 모드.
- 캐논 제약(§2): 생활안전센터=지방 신고 창구(군사기관 아님), ORACLE 실체 초반 비노출,
  감염 원장 "완전 삭제" 단정 금지, 공공기관 단일 악역화 금지.
- **출시 후 반응 보고 결정.** 하려면 "회색 버튼" 1개로 소규모 검증부터.

## 착수 전 사람이 결정할 항목 (제안서 §15)

1. 후속 카드: 기존 풀 vs 별도 예약 큐 (→ R3에서 예약 큐 권장)
2. 사건ID와 카드ID 분리 방식
3. LOG 조사 보드: 텍스트 목록으로 시작 vs 풀 UI
4. 아카이브 해금 조건 저장 위치
5. 선택 경로 재플레이 간 공유 vs 세이브별 분리
6. 실패 기록 즉시 vs ARC 종료 시
7. 제한 기록: 존재 숨김 vs 빈 슬롯 노출
8. 이미지 출처 라벨 전체 vs 특정 카드
9. 첫 테스트 ARC = 제12생활구 확정 여부
10. P0 실험 성공 기준

---
*이 문서는 출시 후 작업 시 재검토한다. 착수 전 캐논(§2)·공개등급 더블체크 필수.*
