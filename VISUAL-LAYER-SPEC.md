# 시각 레이어 3종 스펙 — 관제 피드 · DAY 전환 · ORACLE 개입

> "텍스트만 읽는 게임 → 보면서 지휘하는 게임" 전환용 3개 기능.
> 공통 원칙: **게임 로직·데이터 흐름은 불변**, 위에 보는 층만 얹는다. 새 이미지 에셋 0장(전부 CSS 도형 + 기존 스타일 재활용). `fxMode==='off'`면 전부 생략(접근성). 모든 연출은 탭으로 즉시 스킵 가능.

---

## 1️⃣ 현장임무 관제 피드 (TACTICAL FEED) — 파일럿: M-007

### 개념
지휘관은 현장에 없다 — **모니터로 보고 무전으로 명령하는 사람**. 기존 노드 텍스트 어드벤처 위에 "관제 모니터" 레이어를 얹는다. 조작(선택지 탭)은 100% 동일.

### 화면 구성 (미션 진행 중, 세로)
```
┌──────────────────────────┐
│ ① TACTICAL FEED          │ 전술 뷰포트 (미니맵 km-* CSS 확장)
│   요원 점 ·· / 목표 ◆     │ 노드 전환 시 점이 경로 따라 이동
│   적 블립 ✕ / 스캔라인    │ (CSS transition 1.5~2s 보간)
├──────────────────────────┤
│ ② ♥ 심박  📶 무전  ⚠위험  │ 팀 상태 스트립 — choices의 risk로 구동
├──────────────────────────┤
│ ③ 무전 로그 (기존 노드 텍스트│ 타이핑/페이드 흐름, 화자별 [강도윤] 태그
├──────────────────────────┤
│ ④ 명령 선택 (기존 choices) │ 변경 없음 (라벨 톤만 "지시"형 유지)
└──────────────────────────┘
```
①②가 신규, ③④는 기존 UI의 스킨.

### 데이터 스키마 — 노드에 선택 필드 `scene` 추가
```js
nodes: {
  start: { text:"...", choices:[...],
    scene: {
      agents:   [{x:15,y:78},{x:18,y:82}],   // % 좌표 (뷰포트 기준)
      target:   {x:72,y:30},                  // 목표 마커 (선택)
      hostiles: [],                           // ✕ 블립 (선택)
      event:    'approach'                    // approach|contact|combat|retreat|hold
    } },
  strike: { ..., scene:{ agents:[{x:55,y:45},{x:58,y:50}], hostiles:[{x:70,y:32}], event:'combat' } }
}
```
- **scene 없는 노드/미션 → 뷰포트 미표시(기존 화면 그대로). 하위호환 100%, 미션별 점진 적용.**
- 렌더러는 이전 scene→새 scene 좌표를 CSS `transition: left/top 1.8s ease`로 보간 → 이동 애니메이션 공짜.
- `event`별 프리셋: `contact`→적 블립 페이드인+핑, `combat`→뷰포트 테두리 붉은 펄스+미세 셰이크, `retreat`→점들이 진입점으로 역이동.

### 상태 스트립 구동 (신규 데이터 불필요)
- 현재 노드 choices의 최고 `risk` 값으로: `VERY HIGH`→심박 붉은 펄스+무전 노이즈 아이콘, `HIGH`→주황, `MEDIUM` 이하→안정 초록.
- 판정 성공/실패 노드 진입 시: 기존 `style-glitch.css` 글리치 0.3s → 결과 표시.

### 구현 체크리스트
- [ ] `components-fieldmission-tactical.js` 신규 (TacticalFeed 컴포넌트, ~150줄)
- [ ] `style-tactical.css` 신규 — `km-*` 미니맵 클래스 패턴 복제·확장 (점/핑/스캔라인/블립)
- [ ] 미션 렌더러에 `node.scene` 있으면 상단 마운트 (기존 hero 이미지 영역과 교대 or 축소 공존 — 파일럿에서 결정)
- [ ] **M-007 전 노드에 scene 데이터 작성 (파일럿)** — start/strike/recon/oracle_tactical/결과 노드들
- [ ] fxMode off → 미마운트 / reduced → 이동 즉시 스냅(트랜지션 제거)
- [ ] 파일럿 검증 후: M-001~010, MI-* 확산 (노드당 scene 1줄이라 데이터 작업 위주)

---

## 2️⃣ DAY 전환 시퀀스 (매일 보는 자리 — 반복 노출 최대)

### 개념
DAY가 넘어갈 때(briefing 진입 직전) **1.5~2초 위성 요약 오버레이**:
어두워짐 → 강원 GRID 지도(기존 미니맵 확장) 위에 오늘의 상태 마커가 찍힘 → `DAY 12 → 13` 타이포 전환 → briefing으로.

### 표시 요소 (전부 기존 상태에서 파생, 신규 데이터 0)
- 봉쇄선 상태 링: `stats.c` 기반 색 (안정=Act색 / 위험=적색 점멸)
- 이변체 활동 마커: `computeMapEvent(stats,logs)` 결과 (warn/attack이면 ✕ 핑)
- 연구 진행 점: `LOG-RES-OPEN` 이후 활성 연구 있으면 램프
- 상단 큰 타이포: `DAY N` → 글자 스크램블 → `DAY N+1` (기존 글리치 텍스트 keyframe 재활용)

### 훅 지점
- `app.js` day+1 세팅(L351/619) 후 `setPhase('briefing')` 흐름 앞에 `setPhase('daycut')` 1단계 삽입 **또는** briefing 컴포넌트 마운트 시 오버레이 선재생(권장 — 페이즈 저장/복원 로직 안 건드림, 새로고침 시 리스크 0).
- 탭하면 즉시 스킵 → briefing. `fxMode off` → 미재생.
- 세이브/리줌 안전: 오버레이는 순수 표시층 — `ts_resumePhase` 등 저장 상태 불변.

### 체크리스트
- [ ] `DayCutOverlay` 컴포넌트 (briefing 내부 선재생 방식, ~80줄) + CSS
- [ ] computeMapEvent/stats 연동 마커 3종
- [ ] 스킵/fxMode/리줌 3케이스 확인

---

## 3️⃣ ORACLE 개입 연출 (감시당하는 느낌의 시각화)

### 개념
ORACLE이 선택을 차단할 때(`card.oracleBlock`, 현재 11장) — 지금은 토스트+카드 셰이크뿐. 여기에 **화면 전체가 0.9~1.2초 ORACLE 색으로 물들며** 차단 문구가 스캔라인 오버레이로 뜨는 연출 추가.

### 시퀀스 (총 ~1.2s)
1. 풀스크린 오버레이 페이드인 0.15s (Act 색 `--ui-rgb` 12% 틴트 + 비네트)
2. 중앙에 `[ORACLE OVERRIDE]` + 차단 메시지, 스캔라인 흐름 (기존 glitchText keyframe)
3. `SFX.play('warn')` (이미 정의된 비프 큐 — 여기서 첫 배선)
4. 페이드아웃 → 기존 oracleShake로 자연 연결

### 훅 지점
- `app.js` L911 `onOracleBlock:function(msg){...}` — 현재 토스트 세팅하는 자리에서 오버레이 state 추가로 올리면 끝. 컴포넌트 1개 + CSS.
- 연속 차단(2·3회) 시 오버레이는 1회차만 풀버전, 이후 짧은 플래시(피로 방지).
- `fxMode off` → 기존 토스트만 / `reduced` → 틴트만(글리치 제거).

### 체크리스트
- [ ] `OracleOverrideOverlay` 컴포넌트 (~50줄) + CSS
- [ ] onOracleBlock 훅 연결 + `SFX.play('warn')` 배선
- [ ] 연속 차단 축약 / fxMode 3분기

---

## 구현 순서 (권장)
1. **3️⃣ ORACLE 개입** — 최소 규모, 훅 1곳, 즉시 체감 → 워밍업
2. **2️⃣ DAY 전환** — 반복 노출 최대, 리스크 낮음(표시층)
3. **1️⃣ 관제 피드 M-007 파일럿** — 최대 규모, 파일럿→확산

## 공통 규칙
- 모든 변경: demo/ 미러, `?v=` 캐시 범프, `node tools/validator.js` 0건, 세이브/리줌 영향 0 확인
- 연출 시간·투명도는 CSS 변수로 뽑아 튜닝 지점 명시
- EN i18n: 오버레이 문구는 `tt()` 경유
